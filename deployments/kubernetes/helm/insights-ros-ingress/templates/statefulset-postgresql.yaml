apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "insights-ros-ingress.fullname" . }}-postgresql
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insights-ros-ingress.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
    app.kubernetes.io/name: postgresql
spec:
  serviceName: {{ include "insights-ros-ingress.fullname" . }}-postgresql
  replicas: 1
  selector:
    matchLabels:
      {{- include "insights-ros-ingress.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: database
      app.kubernetes.io/name: postgresql
  template:
    metadata:
      labels:
        {{- include "insights-ros-ingress.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: database
        app.kubernetes.io/name: postgresql
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: postgres
          image: "{{ .Values.database.insights.image.repository }}:{{ .Values.database.insights.image.tag }}"
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          ports:
            - name: postgres
              containerPort: {{ .Values.database.insights.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "insights-ros-ingress.fullname" . }}-postgresql
          env:
            - name: POSTGRES_DB
              value: {{ .Values.database.insights.name | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.database.insights.username | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "insights-ros-ingress.fullname" . }}-db-credentials
                  key: insights-password
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.database.insights.username }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.database.insights.username }}
            initialDelaySeconds: 5
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
          resources:
            {{- toYaml .Values.resources.database | nindent 12 }}
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: {{ .Values.global.storageClass }}
        resources:
          requests:
            storage: {{ .Values.database.insights.storage.size }}